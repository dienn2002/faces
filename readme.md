py -3.10 -m venv venv   
pip install deepface tensorflow-cpu==2.10.0 --no-cache-dir -i https://pypi.tuna.tsinghua.edu.cn/simple
pip uninstall numpy
pip install numpy==1.26.4
pip uninstall opencv-python
pip install opencv-python==4.5.5.64
pip install PyQt6 requests Pillow

lay cai ảnh bỏ qua cái tề
khi nịa m dùng hàm chi đó
Vưt vô cái
T search hắn không ăn
Cái load trước model á
Ngất"Rứa thì mô phải là lòa trước
Thoi cứ gọi rứa đi
ver cũ chưa sp cái model nớ
